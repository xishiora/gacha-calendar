/* Must contain a calendars model with similarities to the one 
 * currently contained in data/calendars.data.json
 * Each object and array must be broken down to 
 * a singular model for ease of implementation
 *
 * Whether the model is accessible from other files is optional, 
 * but implementation of getters is not a bad thing.
 **/